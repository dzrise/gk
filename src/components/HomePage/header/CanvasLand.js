import React, { Component } from 'react';
import ReactDOM from "react-dom";
import * as THREE from "three";

export default class CanvasLand extends Component {
    render() {
        let styleCanvas = {

        }
        return (
            <div>

            </div>
        );
    }
}
